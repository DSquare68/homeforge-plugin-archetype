<?xml version="1.0" encoding="UTF-8"?>
<archetype-descriptor xmlns="http://maven.apache.org/plugin-archetype/archetype-descriptor/1.1.0"
                       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                       xsi:schemaLocation="http://maven.apache.org/plugin-archetype/archetype-descriptor/1.1.0 http://maven.apache.org/xsd/archetype-descriptor-1.1.0.xsd"
                       name="homeforge-plugin">

    <requiredProperties>
        <!-- URL path served by HUB for this plugin, e.g. /gym -> https://hub.local/gym -->
        <requiredProperty key="pluginPath">
            <defaultValue>/my-plugin</defaultValue>
        </requiredProperty>

        <!-- PostgreSQL schema dedicated to this plugin's tables -->
        <requiredProperty key="pluginSchema">
            <defaultValue>my_plugin_schema</defaultValue>
        </requiredProperty>

        <!-- Stable snake_case identifier, also used as the Flyway schema-history table prefix -->
        <requiredProperty key="pluginId">
            <defaultValue>my_plugin</defaultValue>
        </requiredProperty>

        <!-- Human readable name shown in the sidebar and plugin manager -->
        <requiredProperty key="pluginName">
            <defaultValue>My Plugin</defaultValue>
        </requiredProperty>

        <!-- Semantic version reported to HUB via PluginMetadata (independent of the Maven ${version}) -->
        <requiredProperty key="pluginVersion">
            <defaultValue>1.0.0</defaultValue>
        </requiredProperty>

        <!-- One-sentence description shown on the plugin management page -->
        <requiredProperty key="pluginDescription">
            <defaultValue>A HUB plugin generated from the homeforge-plugin-archetype.</defaultValue>
        </requiredProperty>
    </requiredProperties>

    <fileSets>
        <fileSet filtered="true" packaged="true" encoding="UTF-8">
            <directory>src/main/java</directory>
            <includes>
                <include>**/*.java</include>
            </includes>
        </fileSet>

        <fileSet filtered="true" packaged="false" encoding="UTF-8">
            <directory>src/main/resources</directory>
            <includes>
                <include>**/*.sql</include>
                <include>**/*.idx</include>
            </includes>
        </fileSet>

        <fileSet filtered="true" packaged="false" encoding="UTF-8">
            <directory></directory>
            <includes>
                <include>README.md</include>
            </includes>
        </fileSet>
    </fileSets>

</archetype-descriptor>
