<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
        Dependencies, pinned versions and the jar/shade build are all inherited.
        <relativePath/> stops Maven looking for the parent on disk - it is
        resolved from the repository, so run 'mvn install' in the
        homeforge-plugin-starter-parent checkout once before building this.
    -->
    <parent>
        <groupId>com.github.dsquare68</groupId>
        <artifactId>homeforge-plugin-starter-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath/>
    </parent>

    <groupId>${groupId}</groupId>
    <artifactId>${artifactId}</artifactId>
    <version>${version}</version>
    <packaging>jar</packaging>

    <name>${pluginName}</name>
    <description>${pluginDescription}</description>

    <properties>
        <!--
            ================================================================
            PLUGIN CONFIGURATION
            ================================================================
            plugin.path        : URL path served by HUB - https://hub.local/${pluginPath}
            plugin.schema      : PostgreSQL schema for this plugin's tables
            plugin.name        : sidebar label and dashboard card title
            plugin.description : one-line summary in the plugin manager

            The parent's maven-jar-plugin config writes all of these into
            MANIFEST.MF, which is where HUB reads a plugin's metadata from
            without loading its classes. Keep PluginInfo.java in sync with them.
            ================================================================
        -->
        <plugin.path>/${pluginPath}</plugin.path>
        <plugin.schema>${pluginSchema}</plugin.schema>
        <plugin.name>${pluginName}</plugin.name>
        <plugin.description>${pluginDescription}</plugin.description>
        <plugin.bootstrap.class>${package}.PluginBootstrap</plugin.bootstrap.class>
    </properties>

    <build>
        <!--
            Coordinates only - all configuration lives in the parent's
            <pluginManagement>.
        -->
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
